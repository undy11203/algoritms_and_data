
typedef enum {
	BadNumberOfLine,
	BadNumberOfVertics,
	BadNumberOfEdges,
	BadVertix
} ErrorStatus;

int InputVerticsAndEdge(int* vertics, int* edges);

int InputWay(int* vertix1, int* vertix2, int maxVertics);
